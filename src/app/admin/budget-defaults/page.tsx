import { BudgetDefaultsClient } from './BudgetDefaultsClient';

export const metadata = {
    title: 'Pre-Dev Budget Defaults | Admin | SLR Pursuits',
};

export default function BudgetDefaultsPage() {
    return <BudgetDefaultsClient />;
}
